# Patient data + generated outputs — never commit (may contain PHI)
patient_case.txt
matches_output.md
referrals.csv
referral_events.csv
referrals/

# Web app: local DB (contains patient notes + accounts), secret, venv
web/bridgemd.db
web/bridgemd.db-shm
web/bridgemd.db-wal
web/bridgemd.db-journal
web/.secret
# Local secrets (API keys, SMTP creds) — never commit
secrets.env
web/secrets.env
.env
web/.env
.venv/
__pycache__/

# Uploaded documents (candidate-thread + team-room files) — runtime data,
# may contain PHI. The directory is auto-created by the app on startup.
web/uploads/

# Overnight eligibility-eval runtime artifacts (branch night/eligibility only).
# Ledger/cache/eval-set/logs are machine-local run state, not source.
web/night_spend.json
web/eval_cache.json
web/eval_set.json
web/eval_test.json
web/match_eval_log.jsonl
web/eval_fails.json
web/eval_fails_dev.json
web/eval_fails_test.json
web/eval_data/
