"""The AI layer: interview, spec generation, extraction, composing, policy,
guardrails and setup changes. Every function works without an LLM key; with
one, llm.py adds the model behind the same interfaces."""
