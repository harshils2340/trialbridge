Flask>=3.0,<4.0
cryptography>=43.0,<47.0
python-dotenv>=1.0,<2.0
pypdf>=4.0
gunicorn>=21.2
# IANA timezone database for zoneinfo. Slim Linux images (Render) often ship
# without system tz data, so ZoneInfo("America/New_York") would raise and the
# calendar would silently fall back to server (UTC) time. This guarantees
# per-user timezones resolve everywhere.
tzdata>=2024.1
